The required `browse` skill is not built in this environment. It needs a one-time build (about 10 seconds), but this session has read-only filesystem access, so I can’t install it here.

Please enable workspace writes or build the skill, then resend the request.
